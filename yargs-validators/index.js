const {
    filterRuntimeYargs,
} = require('./filter-runtime');
const {
    filterRatingYargs,
} = require('./filter-rating');
const {
    filterLanguageYargs,
} = require('./filter-language');
const {
    filterGenreYargs,
} = require('./filter-language');
const {
    searchYargs,
} = require('./search');
const {
    sortYargs,
} = require('./sort');
module.exports = {
    filterRuntimeYargs,
    filterRatingYargs,
    filterLanguageYargs,
    filterGenreYargs,
    searchYargs,
    sortYargs,
};
